[![crates.io](https://img.shields.io/crates/v/corelib.svg)](https://crates.io/crates/corelib) [![docs.rs](https://img.shields.io/docsrs/corelib.svg)](https://docs.rs/corelib)

# corelib — the 8b-is corelib

One import for the engine's core: **world-core** (the ternary wire, GAIA,
the keeper's fold, the transport ring + ledger, the ultra-graphs), the
**1.58-bit lane** (`ternary`: the `.tern` checkpoint, the bit-exact
integer GEMM, the seeded dream), and the **qdecorators** kit — all
re-exported behind a single facade.

```rust
use corelib::*;
let grapah = UltraGraph::from_seed("sanctuary", 12, 0.35);
let dream = dream_continuation("the world runs without", alphabet, 64, 0.8);
```

Published on crates.io, semver-managed from `1.9.0`.

*the constellation · 0 + 1 · fine touch from within · vaked.dev*
