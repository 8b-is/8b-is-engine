[package]
name = "corelib"
description = "the 8b-is corelib: one import for the engine's core — world-core (GAIA, the fold, the transport), the 1.58-bit lane (ternary), and the qdecorators kit"
version = "1.9.0"
edition = "2021"
license = "MIT"
authors = ["the constellation <cabotage@pm.me>"]
readme = "README.md"
repository = "https://github.com/peterlodri-sec/8b-is-engine"
keywords = ["engine", "world", "ternary", "functional"]
categories = ["game-engines"]
publish = true

[dependencies]
world-core = { path = "../world-core", version = "1.9.0" }
ternary-lane = { path = "../ternary", version = "1.9.0" }
qdecorators = { path = "../qdecorators", version = "1.9.0" }
