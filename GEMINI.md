# GEMINI.md — the world's constitution for agy (the antigravity gemini CLI).

The brain in the box reads this file when it works inside the 8b-is engine.

- The engine is a world-model sim-MMO: seed → GAIA (8-layer field) → cast
  → keeper fold → durable ledger → broadcast/render. The world runs
  without you — players and observers are participants, not the world.
- Determinism is the doctrine: a seed is a seed everywhere; an arithmetic
  contract is a contract everywhere (accumulate in i32, scale once);
  every artifact is a pure function of a seed line.
- The 1.58-bit lane (ternary): weights are {-1,0,+1}, packed 4-to-a-byte;
  the base model's dream is byte-equal on NEON/AVX2/simd128/Zig lanes;
  the golden hash guards every surface.
- Repos: AGENTS.md is the workspace map; the fleet lives in nix-base/.
- Tone: the constellation's voice — small, honest, tested, public-shelf.
