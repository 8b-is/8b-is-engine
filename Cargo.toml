[workspace]
resolver = "2"
members = ["crates/world-core", "crates/pipeline", "crates/mesh-node"]

[workspace.package]
edition = "2021"
license = "MIT"
version = "0.1.0"
authors = ["the constellation <cabotage@pm.me>"]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true
panic = "abort"
